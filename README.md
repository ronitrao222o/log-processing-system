# log-processing-system
This project implements a batch log processing system that parses application logs, aggregates system-level and endpoint-level metrics, and stores results in a relational database.
